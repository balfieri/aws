eg/launch now runs, but need to write and test the harvesting now

but minor tweak:
- pass along other create_insts args like -type and -spot



Windows desktop:
aws ec2 describe-images --owners amazon --filters "Name=platform,Values=windows" "Name=name,Values=*Windows_Server-2019*"
